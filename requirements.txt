# Core ML — install torch first: pip install torch --index-url https://download.pytorch.org/whl/cu124
torch>=2.6.0
transformers>=4.45.0
datasets>=2.19.0
accelerate>=0.27.0

# LoRA adapter loading
peft>=0.10.0
bitsandbytes>=0.43.0

# Vision
Pillow>=10.0.0
timm>=0.9.0

# Utilities
numpy>=1.24.0
tqdm>=4.65.0
pandas>=1.3.0
pyarrow>=12.0.0
scikit-learn>=1.0.0

# HEDGE/VASE scoring (scripts/03_generate_hedge.py only)
hedge-bench
sentence-transformers>=2.0.0

# Optional: Flash Attention (recommended for faster inference)
# pip install flash-attn --no-build-isolation
